export default function OrdersPage() {
  return (
    <div className="p-4 space-y-2">
      <h2 className="text-xl font-semibold">Pedidos</h2>
      <p>
        Aquí irá la tabla de pedidos (pendiente, en vuelo, entregado) con filtros
        y acciones. Integra tu endpoint <code>GET /orders</code> cuando el backend
        esté listo.
      </p>
    </div>
  )
}
